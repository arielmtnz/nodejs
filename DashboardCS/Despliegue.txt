npm install dotenv
npm install pm2 -g


cd c:\repositorios\nodejs\dashboardcs
pm2 update
pm2 start server.js --name DashboardCenturySystems
pm2 restart DashboardCenturySystems --update-env
pm2 install pm2-windows-startup
pm2-startup install      =>con permisos de administrador
pm2 uninstall pm2-windows-startup
pm2 status



1. Instalar Node.js
2. Instalar PM2: npm install pm2 -g
3. Clonar o copiar zip del proyecto en una carpeta.
4.  cd C:\ruta\de\tu\proyecto
	npm install --production
5. 	


NGINX
======
server {
    listen 80;
    server_name dashboardcenturysystems;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

C:\Windows\System32\drivers\etc\hosts
127.0.0.1   dashboardcenturysystems


taskkill /F /IM nginx.exe


start nginx
nginx -s stop
nginx -s reload


netstat -ano | findstr :80
tasklist | findstr nginx
taskkill /F /IM nginx.exe
sc query type= service | findstr /I "nginx"
sc stop nginx
sc delete nginx
sc query type= service | findstr /I "nginx"







NSSM
======
1. Descargar 
2. C:\nssm\nssm.exe edit nginx



CMD as Administrator
======================
net start nginx
sc query nginx